FOO {
  # explicitly list symbols to be exported
  global:

    lldpkg_create;
    lldpkg_destroy;
    lldpkg_push_buffer;
    lldpkg_get_media_time;
    lldpkg_get_version;

  # hide everything else
  local: *;
};
