FOO {
  # explicitly list symbols to be exported
  global:

    lldpkg_create;
    lldpkg_destroy;
    lldpkg_push_buffer_ext;
    lldpkg_get_media_time_ext;
    lldpkg_get_version;

  # hide everything else
  local: *;
};
